# Web

React web client for SubTrack.
Contains auth, subscriptions, analytics and notifications pages.
Runs with Vite.

Subscriptions page includes quick support actions:
- `Quick cancel`
- `Quick pause`

Both actions fetch backend support email draft and provide:
- open mail client (`mailto`)
- copy full email text
